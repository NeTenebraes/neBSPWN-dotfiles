#
# wm independent hotkeys
#

# Captura toda la pantalla
Print
    maim | xclip -selection clipboard -t image/png

shift + Print
    maim -s | xclip -selection clipboard -t image/png

ctrl + Print 
    bash -c "maim ~/Pictures/screenshots/screenshot_$(date +'%Y-%m-%d_%H-%M-%S').png"

# terminal emulator
super + Return
    kitty
# program launcher
super + d
	rofi -show drun -show-icons

# make sxhkd reload its configuration files:
super + shift + r
	pkill -USR1 -x sxhkd

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

# close and kill
super + {_,shift + }q
	bspc node -{c,k}


#
# state/flags
#

# set the window state
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# alternate between the tiled and monocle layout
super + space
	bspc desktop -l next


#
# focus/swap
#

super + {_,shift + }{Left,Down,Up,Right}
       bspc node -{f,s} {west,south,north,east}


# focus the last node/desktop
super + {grave,Tab}
	bspc {node,desktop} -f last

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

#
# move/resize
#

# Expandir
super + ctrl + {Left,Down,Up,Right}  
      ~/.config/bspwm/bspwm_resize expand {west,south,north,east} 50

# Contraer  
super + alt + {Left,Down,Up,Right}
      ~/.config/bspwm/bspwm_resize contract {west,south,north,east} 50


# move a floating window
super + ctrl + alt + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}a


# NO USED

# preselect the direction
#super + ctrl + alt + {Left,Down,Up,Right}
#	bspc node -p {west,south,north,east}

# cancel the preselection for the focused node
#super + ctrl + space
#	bspc node -p cancel

# set the node flags
#super + ctrl + {m,x,y,z}
#	bspc node -g {marked,locked,sticky,private}